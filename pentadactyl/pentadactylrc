map -b ; :
map -b : ;
map -b j 3j<CR>
map -b k 3k<CR>
map -b J :bp<CR>
map -b K :bn<CR>
map -b <Up> <C-v><Up><CR>
map -b <Down> <C-v><Down><CR>
map -b <Left> <C-v><Left><CR>
map -b <Right> <C-v><Right><CR>

colorscheme solarized-light

" customize fonts
highlight FontFixed -append font-size: 11pt !important;
highlight -a Hint font-size: 11pt !important;
highlight -a Hint background-color: yellow !important;

" only use the following set of keys to generate link hints
set hintkeys="asdfghjkl"
